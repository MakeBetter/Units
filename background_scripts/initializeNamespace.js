// JSHint config
/* exported _u */

// This is the first background script to load (see manifest.json for the order in which background scripts are loaded).
// It simply initializes the following object, which will serve as the top-level namespace for Units Project (for
// background scripts)

var _u = {};
